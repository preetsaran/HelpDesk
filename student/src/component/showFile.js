import React from 'react'
import PropTypes from 'prop-types'

const showFile = ({fileUploaded}) => {
    return (
        <div>
            
        </div>
    )
}

showFile.propTypes = {
  fileUploaded:PropTypes.object.isRequired,
}

export default showFile
